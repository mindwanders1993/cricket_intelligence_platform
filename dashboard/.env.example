# Copy to .env (gitignored) and adjust paths as needed.
# Python data loaders in src/data/*.csv.py read this at build time:
#   os.environ.get("DUCKDB_PATH", "../storage/duckdb/cricket.duckdb")
# Path is relative to the dashboard/ directory.
DUCKDB_PATH=../storage/duckdb/cricket.duckdb
